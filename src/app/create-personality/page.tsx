import { AgentBuildPersonality } from "@/components/Create/AgentBuildPersonality";

export default function CreatePersonalityPage() {
  return (
    <main className="min-h-screen w-screen overflow-hidden bg-gradient-to-br from-white via-gray-50 to-blue-50">
      <AgentBuildPersonality />
    </main>
  );
}
